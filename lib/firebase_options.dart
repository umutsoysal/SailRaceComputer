import 'package:firebase_core/firebase_core.dart';

/// Placeholder until `flutterfire configure` generates real platform options.
///
/// Keep this file tracked so the app compiles before Firebase is configured.
/// The FlutterFire CLI will replace it with a generated version for your
/// actual Firebase project.
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'Firebase is not configured yet. Run `flutterfire configure` '
      'from the project root to generate real Firebase options.',
    );
  }
}
