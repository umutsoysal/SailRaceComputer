name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  package-android-web:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Verify release version
        run: dart run tool/verify_release_version.dart "${GITHUB_REF_NAME}"

      - name: Decode Android keystore
        if: ${{ secrets.ANDROID_KEYSTORE_BASE64 != '' }}
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > android/upload-keystore.jks

      - name: Write Android signing properties
        if: ${{ secrets.ANDROID_KEYSTORE_BASE64 != '' }}
        run: |
          cat > android/key.properties <<EOF
          storeFile=upload-keystore.jks
          storePassword=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}
          keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
          EOF

      - name: Build Android and Web artifacts
        run: |
          flutter build apk --release
          flutter build appbundle --release
          flutter build web --release --base-href "/"

      - name: Collect release assets
        run: |
          mkdir -p release-assets
          cp build/app/outputs/flutter-apk/app-release.apk release-assets/race-mate-${GITHUB_REF_NAME}.apk
          cp build/app/outputs/bundle/release/app-release.aab release-assets/race-mate-${GITHUB_REF_NAME}.aab
          tar -czf release-assets/race-mate-web-${GITHUB_REF_NAME}.tar.gz -C build/web .

      - name: Upload packaged Android and Web assets
        uses: actions/upload-artifact@v4
        with:
          name: release-assets-android-web
          path: release-assets/*
          if-no-files-found: error

  package-ios:
    runs-on: macos-latest
    timeout-minutes: 40

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Verify release version
        run: dart run tool/verify_release_version.dart "${GITHUB_REF_NAME}"

      - name: Build unsigned iOS release bundle
        run: flutter build ios --release --no-codesign

      - name: Archive iOS app bundle
        run: |
          mkdir -p release-assets
          ditto -c -k --sequesterRsrc --keepParent build/ios/iphoneos/Runner.app release-assets/race-mate-ios-${GITHUB_REF_NAME}-unsigned.zip

      - name: Upload packaged iOS asset
        uses: actions/upload-artifact@v4
        with:
          name: release-assets-ios
          path: release-assets/*
          if-no-files-found: error

  publish:
    runs-on: ubuntu-latest
    needs:
      - package-android-web
      - package-ios
    timeout-minutes: 15

    steps:
      - name: Download packaged assets
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          pattern: release-assets-*
          merge-multiple: true

      - name: Generate checksums
        run: |
          shasum -a 256 release-assets/* > release-assets/SHA256SUMS.txt

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          generate_release_notes: true
